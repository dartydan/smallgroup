# Vercel environment variables — all required for this project
# Copy to .env.vercel, fill in values, then Vercel → Settings → Environment Variables → Import
# .env.vercel is gitignored.

# 1. Database (Supabase: Project Settings → Database → Connection string, URI mode)
DATABASE_URL=postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres

# 2. Supabase auth (Project Settings → API: Project URL + Publishable key)
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxx

# 3. Your Vercel URL (set after first deploy, then redeploy so the web app uses it)
EXPO_PUBLIC_API_URL=https://your-project.vercel.app

# 4. Same Supabase values for the Expo web build (required for copy-expo-web step)
EXPO_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxx
